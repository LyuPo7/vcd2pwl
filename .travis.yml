env:
  global:
    - CC_TEST_REPORTER_ID=46d00716b076adb48579bc38af34980bf12e0ce5cb3b2a5165b17fba5adb3153

language: python
python: 3.6

before_install:
  # make depends on poetry
  - pip install poetry

install:
  - make install

before_script:
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  - chmod +x ./cc-test-reporter
  - ./cc-test-reporter before-build

script: 
  - make check

after_script: 
  - ./cc-test-reporter after-build